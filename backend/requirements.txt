# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Settings & validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# Database (async) + migrations
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
greenlet==3.1.1

# Auth — verify Supabase-issued JWTs
pyjwt[crypto]==2.10.1
# Encrypt Google refresh tokens at rest (also pulled by pyjwt[crypto], pinned explicitly)
cryptography==44.0.0

# LLM (Groq is OpenAI-compatible; we call via httpx for full control of key rotation)
httpx==0.28.1

# Background jobs (no Redis)
apscheduler==3.11.0

# RAG — policy knowledge base (semantic search)
pgvector==0.3.6
fastembed==0.5.1          # lightweight ONNX embeddings (no torch), self-contained
pypdf==5.1.0              # extract text from uploaded PDF policies
